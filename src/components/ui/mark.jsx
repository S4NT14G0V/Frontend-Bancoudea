import { Mark, Text } from "@chakra-ui/react"

export const MarkComp = () => {
  return (
    <Text>
      The <Mark variant="subtle">design system</Mark> is a collection of UI
      elements
    </Text>
  )
}
